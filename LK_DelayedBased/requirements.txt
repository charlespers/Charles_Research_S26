numpy>=2.0
matplotlib>=3.5
scikit-learn>=1.0
pandas>=1.3
joblib>=1.2
scipy>=1.7

# GPU acceleration (optional — required only for --gpu flag)
# Install ONE of the following depending on your CUDA version:
#   CUDA 12.x:  pip install "jax[cuda12]"
#   CPU only:   pip install jax
